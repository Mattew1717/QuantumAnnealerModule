# Direct dependencies of the experiment scripts (Inference/, NeuralNetwork/).
# The installable package's own dependencies are declared in pyproject.toml.
torch>=2.0
numpy>=1.24
dimod>=0.12.0
dwave-neal>=0.6.0
dwave-system>=1.30.0
networkx>=3.0
scikit-learn>=1.3
scipy>=1.10
pandas>=2.0
matplotlib>=3.9
seaborn>=0.13
python-dotenv>=1.0

# The full_ising_model package itself (editable install from src/)
-e .
