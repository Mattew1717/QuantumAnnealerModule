\documentclass{ieeeaccess}
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{caption}
\usepackage{float}
\usepackage{soul}
\usepackage{algpseudocode}
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}
\begin{document}
\history{Date of publication xxxx 00, 0000, date of current version xxxx 00, 0000.}
\doi{10.1109/TQE.2020.DOI}

\title{Preparation of Papers for IEEE Transactions on Quantum Engineering (March 2020)}
\author{\uppercase{First A. Author}\authorrefmark{1}, \IEEEmembership{Fellow, IEEE},
\uppercase{Second B. Author\authorrefmark{2}, and Third C. Author,
Jr}.\authorrefmark{3},
\IEEEmembership{Member, IEEE}}
\address[1]{National Institute of Standards and 
Technology, Boulder, CO 80305 USA (email: author@boulder.nist.gov)}
\address[2]{Department of Physics, Colorado State University, Fort Collins, 
CO 80523 USA (email: author@lamar.colostate.edu)}
\address[3]{Electrical Engineering Department, University of Colorado, Boulder, CO 
80309 USA}
\tfootnote{This paragraph of the first footnote will contain support 
information, including sponsor and financial support acknowledgment. For 
example, ``This work was supported in part by the U.S. Department of 
Commerce under Grant BS123456.''}

\markboth
{Author \headeretal: Preparation of Papers for IEEE Transactions on Quantum Engineering}
{Author \headeretal: Preparation of Papers for IEEE Transactions on Quantum Engineering}

\corresp{Corresponding author: First A. Author (email: author@ boulder.nist.gov).}

\begin{abstract}
...
\end{abstract}

\begin{keywords}
Enter key words or phrases in alphabetical 
order, separated by commas. For a list of suggested keywords, send a blank 
email to keywords@ieee.org or visit \underline
{http://www.ieee.org/organizations/pubs/ani\_prod/keywrd98.txt}
\end{keywords}

\titlepgskip=-15pt

\maketitle

\newcommand{\added}[1]{\textcolor{green}{#1}}
\newcommand{\removed}[1]{{\textcolor{red}{\st{#1}}}}

\section{Introduction}
code: https://github.com/Mattew1717/QuantumAnnealerModule

\clearpage
\section{Background}%1-1.5 pages
In this section we aim at introducing the background required to understand the proposed method, focusing on the Ising Model and its realization through quantum annealing, as well as the learning process the method is based on.

\subsection{The Ising Model}
The Ising model provides a mathematical framework originally introduced to describe ferromagnetic systems. It is defined on a graph \( (V, E) \), where each vertex \( i \in V \) represents a discrete spin variable \( z_i \in \{-1, +1\} \). Each spin is associated with a local bias \( \theta_i \in \mathbb{R} \) and pairwise couplings \( \Gamma_{ij} \in \mathbb{R} \) for each edge \( (i, j) \). The total energy of a configuration is given by the Hamiltonian:
\begin{equation}\label{eq:ising}
    E(\theta, \Gamma, z) = \sum_{i \in V} \theta_i z_i + \sum_{(i,j) \in E} \Gamma_{ij} z_i z_j.
\end{equation} 

An Ising machine seeks the spin configuration that minimizes this Hamiltonian:
\begin{equation}
z^* = \arg\min_z E(\theta, \Gamma, z),
\end{equation}
where $z^*$ corresponds to the ground state configuration. This minimization process can be used to encode a wide class of combinatorial optimization problems, since many discrete problems can be expressed in the form of the Ising model.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.45\textwidth]{isingModel.png}
    \caption{On the left, an illustration of the graph structure of an Ising model 
        characterized by a fully connected graph, with \( |V| = 5 \) spins \( z \), 
        corresponding biases \( \theta \), and couplings \( \Gamma \). 
        An Ising machine maps the Ising model to the right-hand side of the figure, 
        returning a \(\{-1, +1\}\) assignment (illustrated as white/black nodes) to each 
        binary variable \( z_i \). The output is the spin configuration \( z^{*} \) 
        and the corresponding minimal energy \( E_{0}(\theta, \Gamma) \).
    }
    \label{fig:isingModel}
\end{figure}

\subsection{Quantum Annealing}
Quantum annealing is a heuristic method for solving optimization problems by evolving a quantum system toward the ground state of a problem Hamiltonian. 
In the context of the Ising model, the optimization problem defined in Eq.~\ref{eq:ising} can be mapped to the ground-state search of a corresponding Hamiltonian.
Specifically, consider the time-dependent Hamiltonian
\begin{equation}
    H(t) = \gamma(t) H_D + H_P
\end{equation}
where $H_P$ is the problem Hamiltonian, $H_D$ is the transverse field Hamiltonian, and $\gamma : \mathbb{R}^+ \to \mathbb{R}$ is a decreasing function that gradually suppresses the kinetic term $H_D$, guiding the system toward the ground state of $H_P$. The transverse field Hamiltonian $H_D$ provides quantum fluctuations that allow the exploration of the solution landscape, while the modulation $\gamma(t)$ controls the annealing process.  

Quantum annealing can be physically realized using a network of qubits arranged on the vertices of a graph $(V, E)$, with $|V| = n$, where the edges $E$ represent the couplings between qubits. The problem Hamiltonian is defined as a self-adjoint operator on the $n$-qubit Hilbert space $\left(\mathbb{C}^2\right)^{\otimes n}$:
\begin{equation}
H_P = \sum_{i \in V} \theta_i \, \sigma_z^{(i)} + \sum_{(i,j) \in E} \Gamma_{ij} \, \sigma_z^{(i)} \sigma_z^{(j)},
\label{hamiltonian}
\end{equation}
where $\sigma_z^{(i)}$ is the Pauli-$z$ operator acting on qubit $i$, and $\theta_i$ and $\Gamma_{ij}$ represent, respectively, the local fields and the couplings between qubits.
Thus, quantum annealing can be viewed as a physical mechanism for minimizing the Ising Hamiltonian.

\subsection{Parametric Model}
The parametric model proposed by Schmid et al. \cite{10.21468/SciPostPhysCore.7.1.013} defines a supervised learning framework based on the principles of Ising machines.
The definition of the model is the following:
\begin{align}
F(\boldsymbol{\theta}|\Gamma, \lambda, \varepsilon) &= \lambda \min_{\mathbf{z} \in \{-1,1\}^n} E(\boldsymbol{\theta}, \Gamma, \mathbf{z}) + \varepsilon \\
&= \lambda E_0(\boldsymbol{\theta}, \Gamma) + \varepsilon.
\label{model}
\end{align}
In this formulation, input data are encoded into the spin biases $\boldsymbol{\theta} = (\theta_1, \ldots, \theta_n)$ of an Ising model, while the trainable parameters correspond to the couplings $\Gamma_{ij}$ determined by the topology of the underlying Ising machine. The model output is defined as the ground-state energy of the system, scaled and shifted through two additional parameters $\lambda$ and $\varepsilon$.

\subsubsection{Training Phase}

Given the training set $\mathcal{D} = \{(\boldsymbol{\theta}^{(a)}, y^{(a)})\}_{a=1,\ldots,N}$, with $y^{(a)} = f(\boldsymbol{\theta}^{(a)})$, where $f : \mathcal{X} \rightarrow \mathbb{R}$ is an unknown function to approximate, the model in \ref{model} can be trained by minimizing a loss function, for example the mean squared error (MSE).
\begin{equation}
L(\Gamma, \lambda, \varepsilon) = \frac{1}{N} \sum_{a=1}^{N} 
\left[ F(\boldsymbol{\theta}^{(a)} | \Gamma, \lambda, \varepsilon) - y^{(a)} \right]^2 
\label{eq:loss}
\end{equation}
The objective is to perform this minimization task by using a gradient descent approach, iteratively updating the parameters $\Gamma$, $\lambda$, and $\varepsilon$ by taking steps in the direction opposite to the gradient of the loss function $L$:
\begin{equation}
\delta \Gamma = -\eta \nabla_{\Gamma} L, \quad
\delta \lambda = -\eta \frac{\partial L}{\partial \lambda}, \quad
\delta \varepsilon = -\eta \frac{\partial L}{\partial \varepsilon},
\label{eq:gradients}
\end{equation}
where $\eta > 0$ is the learning rate controlling the optimization step size.


This representation highlights a neural-like structure, where spins act as neurons and couplings as synaptic weights. Notably, the learning process does not require exact ground-state solutions; approximate energies produced by the Ising machine can be effectively integrated into the optimization, as their systematic deviations are absorbed by the parameter tuning process. This property makes the model practical and robust for real-world training scenarios.


\clearpage
\section{Proposal}
\subsection{Quantum Ising Modules in Neural Networks}

Integrating quantum-inspired optimization models within neural network architectures opens new avenues for solving complex, high-dimensional problems. In this work, we propose a framework where quantum Ising models are embedded as differentiable modules within a neural network, combining the combinatorial optimization capabilities of Ising machines with the function approximation power of deep learning.

\subsubsection{Motivation}

Standard neural networks rely on continuous differentiable mappings to propagate gradients during training. However, many real-world optimization problems are discrete in nature and can be naturally expressed as spin systems in the form of Ising Hamiltonians. Quantum annealers, or their classical simulations, provide a mechanism to efficiently explore the combinatorial space and approximate ground-state solutions of these Hamiltonians. Embedding such mechanisms into neural networks allows us to leverage this optimization power while retaining end-to-end differentiability for gradient-based learning.

\subsubsection{Differentiable Ising Energy Function}

To integrate an Ising-based module within a neural network, we define a custom differentiable function that computes the ground-state energy of an Ising Hamiltonian given a batch of input biases. For each input vector $\theta$, representing a set of local fields on the spins, the module uses an annealing solver to find an approximate ground-state configuration \(z^*\) that minimizes the Hamiltonian:

\[
E_0(\theta, \Gamma) = \min_{z \in \{-1,+1\}^n} \left( \sum_i \theta_i z_i + \sum_{i<j} \Gamma_{ij} z_i z_j \right),
\]

where $\Gamma$ are the learnable couplings encoding the interactions between spins. By storing the resulting spin configurations during the forward pass, the module can propagate gradients with respect to $\Gamma$ in the backward pass using a surrogate gradient:

\[
\frac{\partial E_0}{\partial \Gamma_{ij}} = z_i^* z_j^*.
\]

This design ensures that the module is fully compatible with automatic differentiation frameworks such as PyTorch, allowing seamless integration with larger neural network architectures.

\begin{algorithm}[H]
\caption{Differentiable Ising Energy Function (Forward and Backward)}
\begin{algorithmic}[1]
\STATE \textbf{Input:} batch of biases $\{\theta_i\}$, coupling matrix $\Gamma$, annealer instance
\STATE \textbf{Forward pass:}
\FOR{each sample $i$ in batch}
    \STATE Convert $\theta_i$ to biases $h_i$
    \STATE Convert $\Gamma$ to coupling matrix $J$
    \STATE Sample approximate ground state $z_i^*$ using annealer
    \STATE Compute energy $E_i = \sum_j h_{i,j} z_{i,j} + \sum_{j<k} J_{jk} z_{i,j} z_{i,k}$
    \STATE Store $z_i^*$ for backward
\ENDFOR
\STATE \textbf{Return:} batch energies $\{E_i\}$
\STATEx
\STATE \textbf{Backward pass:}
\FOR{each sample $i$ in batch}
    \STATE Compute outer product $z_i^* (z_i^*)^\top$
    \STATE Multiply by incoming gradient $\partial L / \partial E_i$
\ENDFOR
\STATE Average over batch and retain upper-triangular part to get $\partial L / \partial \Gamma$
\STATE \textbf{Return:} $\partial L / \partial \Gamma$
\end{algorithmic}
\end{algorithm}

\subsubsection{Full Ising Module}

While the \texttt{IsingLayer} computes the Ising ground-state energy, the \texttt{FullIsingModule} extends this functionality by introducing two additional learnable parameters: a scaling factor $\lambda$ and an energy offset. The module output is defined as:

\[
E_\text{out} = \lambda E_0 + \text{offset}.
\]

This linear transformation allows the Ising module to match the scale of the target outputs in a supervised learning context, providing flexibility analogous to the activation and bias terms in traditional neural network layers. Consequently, the module can be trained end-to-end, learning both the spin couplings and the  scaling parameters.

\begin{algorithm}[H]
\caption{Forward Pass of Full Ising Module}
\begin{algorithmic}[1]
\STATE Compute $E_0$ using Ising Energy Function
\STATE Scale and shift: $E_\text{out} = \lambda \cdot E_0 + \text{offset}$
\STATE \textbf{Return:} $E_\text{out}$
\end{algorithmic}
\end{algorithm}

\subsubsection{Parallelized Computation and Scalability}

The computation of ground-state energies for a batch of inputs can be efficiently parallelized. By splitting the batch across multiple threads, each thread computes the annealing solution for a subset of inputs. This allows leveraging multicore CPUs or distributed systems for scalability, ensuring that the computational overhead of embedding an Ising solver within a neural network remains manageable even for moderately large spin systems.

\subsubsection{Integration into Neural Networks}
Embedding the Full Ising Module in a neural network effectively creates a hybrid model where discrete combinatorial optimization is combined with continuous learning. The neural network can supply input biases to the Ising module, which then computes a structured, discrete representation of the energy landscape. Gradients flow backward from the loss function through the Ising module to update both the couplings and the scaling parameters. This enables the network to learn complex mappings that require both discrete reasoning and continuous function approximation.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.45\textwidth]{nn.png}
    \caption{An illustration of single layer neural network.
    }
    \label{fig:isingModel}
\end{figure}

\subsubsection{Single-Layer Ising Neural Network}\label{sec:singlelayer}
In addition to a single \texttt{FullIsingModule}, we consider a shallow architecture composed of $M$ parallel Full Ising Modules whose outputs are linearly combined. This structure can be interpreted as a single-layer neural network where each unit is an independent Ising-based energy model.

Given an input vector $\boldsymbol{\theta}$, the $m$-th \texttt{FullIsingModule} produces an output
\begin{equation}
F^{(m)}(\boldsymbol{\theta}) \;=\; \lambda^{(m)} \, E_0\!\big(\boldsymbol{\theta}, \Gamma^{(m)}\big) \;+\; \varepsilon^{(m)},
\label{eq:fullising_out}
\end{equation}
where $E_0(\boldsymbol{\theta}, \Gamma^{(m)})$ denotes the ground-state energy of the Ising Hamiltonian with input biases $\boldsymbol{\theta}$ and trainable couplings $\Gamma^{(m)}$, while $\lambda^{(m)}$ and $\varepsilon^{(m)}$ are learnable scaling and offset parameters.

The single-layer network with $M$ parallel modules aggregates their outputs through a linear readout:
\begin{equation}
F(\boldsymbol{\theta}) \;=\; b \;+\; \sum_{m=1}^{M} w_m \, F^{(m)}(\boldsymbol{\theta}),
\label{eq:linear_comb_fullising}
\end{equation}
with $w_m$ and $b$ being additional trainable parameters.

This architecture is analogous to a single fully connected layer in standard neural networks, with the key difference that each neuron is replaced by a structured Ising energy minimization block. The nonlinearity arises from the discrete ground-state computation within each module, while the outer linear combination enables flexible aggregation of multiple energy-based representations.

Despite its shallow structure, this model increases expressive power compared to a single Ising module, as different modules can learn different coupling patterns, effectively acting as an ensemble of energy-based learners trained end-to-end.
% ============================================================
%  Section: Empirical Evaluation Single vs NN
% ============================================================

\section{Empirical Evaluation}\label{sec:empirical}

This section empirically contrasts the single \texttt{FullIsingModule}
with the shallow \texttt{MultiIsingNetwork} (an ensemble of $M=5$ parallel
\texttt{FullIsingModule}s combined through a linear readout). The
comparison is articulated in three steps: (i) UCI benchmarks consumed
in their native imbalanced form, (ii) the same UCI benchmarks evaluated
on pre-balanced fixed folds, and (iii) synthetic $d$-dimensional XOR
problems used as a controlled probe of non-linear capacity. The first
two steps share datasets and pipeline and differ only in the data
preparation, isolating the impact of class imbalance on Ising-based
learners; the XOR step then exposes the asymptotic capacity gap between
the two architectures.

% ------------------------------------------------------------
\subsection{Method}\label{sec:method}
% ------------------------------------------------------------

\subsubsection{Datasets}\label{sec:datasets}

Nine binary classification datasets from the UCI Machine Learning
Repository~\cite{Dua:2019} are adopted, spanning feature
dimensionalities from 3 to 44 and sample sizes from 99 to 1\,372 in
order to assess robustness and generalization across heterogeneous
regimes. The UCI suite is complemented by a family of synthetic
$d$-dimensional XOR datasets for $d \in \{1,\dots,6\}$, which serve as
a controlled stress test for higher-order non-linear interactions.
Table~\ref{tab:datasets} summarizes the size and per-class distribution
of both groups.

\begin{table}[H]
\centering
\caption{Summary of the datasets used in the experimental evaluation.}
\label{tab:datasets}
\begin{tabular}{lccc}
\hline
Dataset                         & \# Features & \# Samples & Class dist.\ (0/1) \\
\hline
00\_iris\_versicolor\_virginica & 4           & 100          & 50 / 50      \\
01\_vertebral\_column           & 6           & 200         & 100 / 100    \\
02\_banknote                    & 4           & 1372        & 762 / 610    \\
03\_breast\_cancer              & 30          & 569         & 357 / 212    \\
04\_contraceptive\_method       & 9           & 962         & 629 / 333    \\
05\_haberman\_survival          & 3           & 306         & 225 / 81     \\
06\_heart\_failure              & 12          & 299         & 203 / 96     \\
07\_ionosphere                  & 34          & 351         & 126 / 225    \\
08\_SPECTF\_Heart               & 44          & 267         & 55 / 212     \\
\multicolumn{4}{c}{\textit{Synthetic XOR datasets}} \\
XOR-1D                          & 1           & 200         & 100 / 100    \\
XOR-2D                          & 2           & 400         & 200 / 200    \\
XOR-3D                          & 3           & 800         & 400 / 400    \\
XOR-4D                          & 4           & 1600        & 800 / 800    \\
XOR-5D                          & 5           & 3200        & 1600 / 1600  \\
XOR-6D                          & 6           & 6400        & 3200 / 3200  \\
\hline
\end{tabular}
\end{table}

\subsubsection{Evaluation Protocol}\label{sec:evalprotocol}

UCI performance is assessed under two complementary regimes that share
the same model, training pipeline and hyperparameters, and differ only
in how the data fed to the cross-validation procedure is constructed.

\paragraph{Original (imbalanced) datasets} Each UCI dataset is consumed
in its native form, with the class distribution reported in
Table~\ref{tab:datasets}. Stratified 5-fold cross-validation ($k=5$)
preserves the original class ratios, so both training and test splits
remain imbalanced. This regime measures how the two architectures behave
when no class-balancing intervention is applied.

\paragraph{Pre-balanced fixed folds (random subsampling)} For each UCI
dataset a class-balanced variant is pre-built by random undersampling
of the majority class, and the resulting CSV is stored on disk. The
same stratified 5-fold splitter is then applied to the already-balanced
files, so every fold contains an equal number of class~0 and class~1
samples in both the training and the test split. Because the folds are
produced from a fixed pre-balanced file and the random seed is fixed
(Table~\ref{tab:hyperparameters}), the splits are deterministic and
shared across the two architectures.

\paragraph{Shared protocol} In both regimes, feature standardization is
fit on the training fold only and applied to the test fold (preventing
information leakage), the random seed is held fixed across runs, and
five metrics are collected and averaged across folds: classification
accuracy, precision, recall, $F_1$ score, and area under the ROC curve
(AUC). For the synthetic XOR problems we use a single stratified
train/test split with the same training pipeline as the UCI experiments.

\subsubsection{Training and Inference Procedure}\label{sec:training}

The training phase is implemented in PyTorch. Both architectures are
trained with the Adam optimizer and the binary cross-entropy with
logits loss (\texttt{BCEWithLogitsLoss}), for a fixed number of epochs
with mini-batch gradient descent. At inference time the sigmoid
activation is applied to the logits to obtain class probabilities, and
a threshold of $0.5$ is used for binary classification.

\subsubsection{Hyperparameter Configuration}\label{sec:hyperparams}

A single, fair set of hyperparameters is shared across all UCI and XOR
experiments (Table~\ref{tab:hyperparameters}); the values were selected
to deliver reasonably good and stable performance across the considered
benchmarks rather than tuned per architecture or per dataset, so that
the comparison reflects architectural differences and not per-task
hyperparameter gains. The configuration mirrors the values logged in
the \texttt{run\_*.log} files produced by the experiment scripts.

\begin{table}[H]
\centering
\caption{Hyperparameters used in all experiments. Values are shared
between the \texttt{FullIsingModule} and the \texttt{MultiIsingNetwork}
unless otherwise noted; the combiner learning rate applies to the
\texttt{MultiIsingNetwork} only, and the K-Fold cross-validation is
used on UCI benchmarks only.}
\label{tab:hyperparameters}
\begin{tabular}{ll}
\hline
Hyperparameter                                  & Value \\
\hline
\multicolumn{2}{l}{\textit{Optimization}} \\
Optimizer                                       & Adam \\
Loss                                            & \texttt{BCEWithLogitsLoss} \\
Number of epochs                                & 200 \\
Batch size                                      & 32 \\
K-Folds (UCI only)                              & 5 \\
Random seed                                     & 42 \\
\hline
\multicolumn{2}{l}{\textit{Per-parameter learning rates}} \\
$\eta_{\Gamma}$ (couplings)                     & $1\!\times\!10^{-3}$ \\
$\eta_{\lambda}$ (energy scale)                 & $5\!\times\!10^{-3}$ \\
$\eta_{\varepsilon}$ (offset)                   & $1\!\times\!10^{-2}$ \\
$\eta_{\text{comb}}$ (linear combiner)          & $5\!\times\!10^{-3}$ \\
\hline
\multicolumn{2}{l}{\textit{Architecture}} \\
Annealer size $n$                               & 50 spins \\
Ising perceptrons $M$ (MultiIsingNetwork)       & 5 \\
Partition input across perceptrons              & False (full input replicated) \\
Hidden-nodes offset $\varepsilon_h$             & $-0.02$ \\
$\Gamma$ initialization                         & $0.0$ \\
$\lambda$ initialization                        & $1.0$ ($\pm 0.1$ per perceptron) \\
$\varepsilon$ initialization                    & $1.0$ ($\pm 0.1$ per perceptron) \\
\# trainable parameters (FullIsingModule)       & 2\,502 \\
\# trainable parameters (MultiIsingNetwork)     & 12\,516 \\
\hline
\multicolumn{2}{l}{\textit{Simulated annealing}} \\
Inverse temperature $\beta$ range               & $[1,\,10]$ \\
Number of reads                                 & 1 \\
Number of sweeps                                & 1\,000 \\
Sweeps per $\beta$ step                         & 1 \\
\hline
\end{tabular}
\end{table}

% ------------------------------------------------------------
\subsection{Results}\label{sec:results}
% ------------------------------------------------------------

We present the empirical findings in three steps, each isolating a
specific aspect of the comparison. Sections~\ref{sec:res_imbalanced}
and~\ref{sec:res_balanced} address the role of class balance on the UCI
benchmarks, while Section~\ref{sec:res_xor} probes the non-linear
capacity of the two architectures on synthetic XOR tasks.

% ============================================================
\subsubsection{UCI Benchmarks --- Original (Imbalanced) Datasets}\label{sec:res_imbalanced}
% ============================================================

This first set of experiments evaluates the two architectures on the
UCI datasets in their native imbalanced form, without any rebalancing
intervention. The aim is to characterize how each model copes with the
class skew typically encountered in real-world data.

\paragraph{Aggregate metrics}
Tables~\ref{tab:ising_uci_imbalanced} and~\ref{tab:nn_uci_imbalanced}
report the per-dataset mean and standard deviation across folds for the
\texttt{FullIsingModule} and the \texttt{MultiIsingNetwork},
respectively. The ensemble matches or substantially outperforms the
single module on every dataset, with the gap widening on the
higher-dimensional benchmarks (\texttt{breast\_cancer},
\texttt{ionosphere}, \texttt{SPECTF\_Heart}). On the most skewed tasks
(\texttt{03\_breast\_cancer}, \texttt{05\_haberman},
\texttt{06\_heart\_failure}, \texttt{08\_SPECTF}) the FullIsingModule
collapses to a constant predictor: its accuracy matches the
majority-class prior almost exactly while precision, recall and $F_1$
drop to zero (or saturate to $1$ on \texttt{08\_SPECTF}, where the
majority class is the positive one). This degeneracy is a clear symptom
of class-imbalance sensitivity that the linear readout of the
MultiIsingNetwork is able to absorb.

\begin{table}[H]
\centering
\caption{FullIsingModule on the original (imbalanced) UCI datasets.
         5-fold CV, mean $\pm$ std.}
\label{tab:ising_uci_imbalanced}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lcccc}
\hline
Dataset                         & Accuracy          & Precision         & Recall            & AUC               \\
\hline
00\_iris\_versicolor\_virginica & $0.720 \pm 0.136$ & $0.707 \pm 0.118$ & $0.740 \pm 0.242$ & $0.766 \pm 0.140$ \\
01\_vertebral\_column           & $0.730 \pm 0.033$ & $0.706 \pm 0.047$ & $0.800 \pm 0.071$ & $0.795 \pm 0.044$ \\
02\_banknote                    & $0.658 \pm 0.030$ & $0.643 \pm 0.061$ & $0.562 \pm 0.171$ & $0.704 \pm 0.034$ \\
03\_breast\_cancer              & $0.627 \pm 0.004$ & $0.000 \pm 0.000$ & $0.000 \pm 0.000$ & $0.480 \pm 0.047$ \\
04\_contraceptive\_method       & $0.708 \pm 0.038$ & $0.687 \pm 0.115$ & $0.316 \pm 0.106$ & $0.714 \pm 0.039$ \\
05\_haberman\_survival          & $0.735 \pm 0.005$ & $0.000 \pm 0.000$ & $0.000 \pm 0.000$ & $0.491 \pm 0.060$ \\
06\_heart\_failure              & $0.669 \pm 0.026$ & $0.040 \pm 0.080$ & $0.010 \pm 0.020$ & $0.400 \pm 0.103$ \\
07\_ionosphere                  & $0.900 \pm 0.031$ & $0.894 \pm 0.040$ & $0.960 \pm 0.009$ & $0.959 \pm 0.027$ \\
08\_SPECTF\_Heart               & $0.794 \pm 0.002$ & $0.794 \pm 0.002$ & $1.000 \pm 0.000$ & $0.720 \pm 0.025$ \\
\hline
\end{tabular}}
\end{table}

\begin{table}[H]
\centering
\caption{MultiIsingNetwork on the original (imbalanced) UCI datasets.
         5-fold CV, mean $\pm$ std.}
\label{tab:nn_uci_imbalanced}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lcccc}
\hline
Dataset                         & Accuracy          & Precision         & Recall            & AUC               \\
\hline
00\_iris\_versicolor\_virginica & $0.930 \pm 0.040$ & $0.933 \pm 0.082$ & $0.940 \pm 0.080$ & $0.948 \pm 0.046$ \\
01\_vertebral\_column           & $0.785 \pm 0.012$ & $0.789 \pm 0.051$ & $0.790 \pm 0.066$ & $0.859 \pm 0.043$ \\
02\_banknote                    & $0.997 \pm 0.004$ & $0.997 \pm 0.007$ & $0.997 \pm 0.007$ & $1.000 \pm 0.000$ \\
03\_breast\_cancer              & $0.851 \pm 0.031$ & $0.831 \pm 0.072$ & $0.764 \pm 0.080$ & $0.927 \pm 0.026$ \\
04\_contraceptive\_method       & $0.730 \pm 0.020$ & $0.636 \pm 0.042$ & $0.528 \pm 0.061$ & $0.771 \pm 0.018$ \\
05\_haberman\_survival          & $0.676 \pm 0.042$ & $0.390 \pm 0.078$ & $0.310 \pm 0.107$ & $0.626 \pm 0.057$ \\
06\_heart\_failure              & $0.689 \pm 0.070$ & $0.523 \pm 0.115$ & $0.460 \pm 0.096$ & $0.673 \pm 0.110$ \\
07\_ionosphere                  & $0.943 \pm 0.016$ & $0.944 \pm 0.010$ & $0.969 \pm 0.023$ & $0.985 \pm 0.010$ \\
08\_SPECTF\_Heart               & $0.824 \pm 0.024$ & $0.864 \pm 0.021$ & $0.925 \pm 0.018$ & $0.815 \pm 0.033$ \\
\hline
\end{tabular}}
\end{table}

\paragraph{Per-fold distribution}
The aggregate statistics are complemented by Figure~\ref{fig:boxplot_imbalanced},
which shows the fold-level distribution of accuracy for both
architectures side by side. The boxes of the MultiIsingNetwork are
systematically shifted upward and are markedly tighter than those of
the FullIsingModule, indicating that the ensemble is not only more
accurate on average but also more stable across folds.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{plots_paper_svg/uci_imbalanced_combined_boxplot}
    \caption{UCI (imbalanced): per-fold accuracy distribution.}
    \label{fig:boxplot_imbalanced}
\end{figure}

\paragraph{Discussion}
Two effects compound on the imbalanced UCI datasets. First, the
expressive power of a single coupling matrix $\Gamma$ is insufficient
to disentangle high-dimensional features. Second, with no rebalancing
the loss is dominated by the majority class, and a single Ising module
has no mechanism to recover from a degenerate prediction. The
MultiIsingNetwork mitigates both issues thanks to its $M=5$ independent
energy-based learners and the learnable linear combiner, which can
re-weight the per-module outputs to compensate for individual
biases.

% ============================================================
\subsubsection{UCI Benchmarks --- Pre-balanced Fixed Folds}\label{sec:res_balanced}
% ============================================================

We repeat the comparison on the same UCI datasets, this time using the
pre-balanced fixed-fold variant described in
Section~\ref{sec:evalprotocol}: every dataset is pre-balanced via random
undersampling of the majority class, and the resulting CSV is fed to the
same 5-fold splitter and the same training pipeline. The per-dataset
metrics are reported in Tables~\ref{tab:ising_uci_balanced}
and~\ref{tab:nn_uci_balanced}. With the class-imbalance shortcut
removed, the apparent accuracy of the FullIsingModule drops from
$\sim$0.73 (imbalanced) to $\sim$0.60 (balanced), and the
MultiIsingNetwork from $\sim$0.82 to $\sim$0.78. The shrinking is
explained by the fact that, on the imbalanced data, both models---and
especially the FullIsingModule---were exploiting the majority class to
inflate accuracy; once each class is equally represented in training
and test, the metrics now reflect genuine discrimination ability rather
than the prior probability of the majority class. Despite the lower
absolute numbers, the MultiIsingNetwork preserves a systematic
advantage over the FullIsingModule on the harder datasets (e.g.\
\texttt{breast\_cancer}, \texttt{contraceptive}, \texttt{SPECTF\_Heart}),
and the previously observed collapses on \texttt{05\_haberman},
\texttt{06\_heart\_failure} and \texttt{08\_SPECTF} disappear: under
balanced data the FullIsingModule produces non-trivial predictions on
every dataset, with non-zero precision, recall and $F_1$.

\begin{table}[H]
\centering
\caption{FullIsingModule on pre-balanced UCI datasets. 5-fold CV on the
         balanced files, mean $\pm$ std.}
\label{tab:ising_uci_balanced}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lcccc}
\hline
Dataset                         & Accuracy          & Precision         & Recall            & AUC               \\
\hline
00\_iris\_versicolor\_virginica & $0.710 \pm 0.092$ & $0.702 \pm 0.069$ & $0.720 \pm 0.194$ & $0.758 \pm 0.138$ \\
01\_vertebral\_column           & $0.725 \pm 0.047$ & $0.703 \pm 0.054$ & $0.790 \pm 0.080$ & $0.799 \pm 0.049$ \\
02\_banknote                    & $0.812 \pm 0.037$ & $0.829 \pm 0.068$ & $0.798 \pm 0.071$ & $0.831 \pm 0.034$ \\
03\_breast\_cancer              & $0.580 \pm 0.056$ & $0.579 \pm 0.056$ & $0.667 \pm 0.166$ & $0.569 \pm 0.078$ \\
04\_contraceptive\_method       & $0.646 \pm 0.053$ & $0.642 \pm 0.053$ & $0.657 \pm 0.068$ & $0.719 \pm 0.052$ \\
05\_haberman\_survival          & $0.431 \pm 0.070$ & $0.452 \pm 0.065$ & $0.546 \pm 0.239$ & $0.462 \pm 0.104$ \\
06\_heart\_failure              & $0.448 \pm 0.039$ & $0.446 \pm 0.037$ & $0.447 \pm 0.125$ & $0.445 \pm 0.047$ \\
07\_ionosphere                  & $0.615 \pm 0.034$ & $0.650 \pm 0.035$ & $0.510 \pm 0.124$ & $0.654 \pm 0.029$ \\
08\_SPECTF\_Heart               & $0.388 \pm 0.047$ & $0.412 \pm 0.041$ & $0.550 \pm 0.150$ & $0.206 \pm 0.012$ \\
\hline
\end{tabular}}
\end{table}

\begin{table}[H]
\centering
\caption{MultiIsingNetwork on pre-balanced UCI datasets. 5-fold CV on
         the balanced files, mean $\pm$ std.}
\label{tab:nn_uci_balanced}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lcccc}
\hline
Dataset                         & Accuracy          & Precision         & Recall            & AUC               \\
\hline
00\_iris\_versicolor\_virginica & $0.910 \pm 0.058$ & $0.921 \pm 0.099$ & $0.920 \pm 0.117$ & $0.958 \pm 0.044$ \\
01\_vertebral\_column           & $0.800 \pm 0.032$ & $0.798 \pm 0.052$ & $0.810 \pm 0.037$ & $0.881 \pm 0.031$ \\
02\_banknote                    & $1.000 \pm 0.000$ & $1.000 \pm 0.000$ & $1.000 \pm 0.000$ & $1.000 \pm 0.000$ \\
03\_breast\_cancer              & $0.835 \pm 0.016$ & $0.832 \pm 0.041$ & $0.844 \pm 0.049$ & $0.898 \pm 0.020$ \\
04\_contraceptive\_method       & $0.670 \pm 0.039$ & $0.689 \pm 0.056$ & $0.637 \pm 0.064$ & $0.743 \pm 0.020$ \\
05\_haberman\_survival          & $0.587 \pm 0.082$ & $0.570 \pm 0.076$ & $0.628 \pm 0.165$ & $0.588 \pm 0.075$ \\
06\_heart\_failure              & $0.630 \pm 0.090$ & $0.639 \pm 0.102$ & $0.615 \pm 0.088$ & $0.682 \pm 0.065$ \\
07\_ionosphere                  & $0.909 \pm 0.053$ & $0.946 \pm 0.036$ & $0.865 \pm 0.083$ & $0.967 \pm 0.019$ \\
08\_SPECTF\_Heart               & $0.650 \pm 0.050$ & $0.631 \pm 0.057$ & $0.750 \pm 0.112$ & $0.753 \pm 0.114$ \\
\hline
\end{tabular}}
\end{table}

The effect of the pre-balanced regime is clearly visible in
Figure~\ref{fig:boxplot_balanced}: the boxplots of the FullIsingModule
shift downward and widen with respect to the imbalanced case,
confirming that the high accuracy observed previously was largely a
consequence of the class prior rather than of genuine learning. The
MultiIsingNetwork remains markedly more stable across folds, and the
gap between the two architectures stays large on the harder tasks.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{plots_paper_svg/uci_balanced_combined_boxplot}
    \caption{UCI (pre-balanced): per-fold accuracy distribution.}
    \label{fig:boxplot_balanced}
\end{figure}

% ============================================================
\subsubsection{Synthetic XOR Datasets}\label{sec:res_xor}
% ============================================================

We close the empirical evaluation with the synthetic
$d$-dimensional XOR problems ($d=1,\dots,6$), where the
ground-truth decision boundary is purely a parity function of the
input bits and any successful model must compose multiple non-linear
interactions. This is the setting in which the architectural gap
between a single Ising module and an ensemble of them becomes most
visible: the FullIsingModule degrades to near chance level already at
$d=3$, whereas the MultiIsingNetwork stays well above $0.85$
accuracy up to $d=6$ (Tables~\ref{tab:ising_xor} and~\ref{tab:nn_xor},
Figure~\ref{fig:bar_compare_xor}).

\begin{table}[H]
\centering
\caption{FullIsingModule on synthetic XOR datasets of increasing
         dimensionality.}
\label{tab:ising_xor}
\begin{tabular}{lcccc}
\hline
Dataset & Accuracy & Precision & Recall & AUC    \\
\hline
XOR-1D  & 0.9250   & 0.9048    & 0.9500 & 0.9925 \\
XOR-2D  & 0.8375   & 0.8293    & 0.8500 & 0.9212 \\
XOR-3D  & 0.5000   & 0.5000    & 0.3500 & 0.5814 \\
XOR-4D  & 0.6531   & 0.6467    & 0.6750 & 0.6783 \\
XOR-5D  & 0.5062   & 0.5037    & 0.8406 & 0.5344 \\
XOR-6D  & 0.5023   & 0.5254    & 0.0484 & 0.5807 \\
\hline
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{MultiIsingNetwork on synthetic XOR datasets of increasing
         dimensionality.}
\label{tab:nn_xor}
\begin{tabular}{lcccc}
\hline
Dataset & Accuracy & Precision & Recall & AUC    \\
\hline
XOR-1D  & 1.0000   & 1.0000    & 1.0000 & 1.0000 \\
XOR-2D  & 0.9750   & 0.9524    & 1.0000 & 1.0000 \\
XOR-3D  & 0.9375   & 0.9375    & 0.9375 & 0.9806 \\
XOR-4D  & 0.9313   & 0.9367    & 0.9250 & 0.9862 \\
XOR-5D  & 0.8562   & 0.8455    & 0.8719 & 0.9257 \\
XOR-6D  & 0.8711   & 0.8350    & 0.9250 & 0.9581 \\
\hline
\end{tabular}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\linewidth]{plots_paper_svg/xor_accuracy_compare}
    \caption{XOR: mean accuracy vs.\ dimensionality, both architectures.}
    \label{fig:bar_compare_xor}
\end{figure}


\section{Discussion}
% TODO

\section{Conclusion}
% TODO


\EOD

\end{document}
