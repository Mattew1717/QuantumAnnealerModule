# Fast configuration for XOR testing
# Use this for rapid prototyping/debugging

PARTITION_INPUT=False
NUM_ISING_PERCEPTRONS=2
MODEL_SIZE=8 # -1 for dataset_size if > 10 else 10.
MINIMUM_MODEL_SIZE=10

# SPEEDUP: Smaller batch requires less computation per step
BATCH_SIZE=128

# SPEEDUP: Reduced epochs (still enough for XOR to converge)
EPOCHS=50

# SPEEDUP: Fewer Ising modules = fewer annealing calls
NUM_ISING_1=2
NUM_ISING_2=2

# SPEEDUP: Fewer samples = smaller dataset
N_SAMPLES_PER_REGION=300
TEST_SIZE=0.2

RANDOM_SEED=42

LAMBDA_INIT=0
OFFSET_INIT=0

LEARNING_RATE_GAMMA=0.01
LEARNING_RATE_LAMBDA=0.01
LEARNING_RATE_OFFSET=0.01
LEARNING_RATE_COMBINER=0.01

# SPEEDUP: Critical - reduce sweeps from 100 to 20
SA_num_reads=1
SA_num_sweeps=20
SA_sweeps_per_beta=1

# SPEEDUP: Validate less frequently during training
VALIDATION_INTERVAL=10
